apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: pippo
  name: pippo
spec:
  replicas: 3
  selector:
    matchLabels:
      app: pippo
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: pippo
    spec:
      containers:
      - image: trenditalydocker/webpage:latest
        name: pippo
        resources: {}
status: {}
---
apiVersion: v1
kind: Service
metadata:
  name: lb-service
  labels:
    app: lb-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
  selector:
    app: pippo
